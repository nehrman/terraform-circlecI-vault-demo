version: 2
jobs:
  Requesting_Azure_credentials_From_vault:
    docker:
      - image: vault:1.1.2
        environment:
          VAULT_ADDR: http://82.64.24.225:8200
    steps:
      - run:
          name: Login to Vault and register token 
          command: |
            apk add jq curl
            tee payload.json <<EOF
            {
              "project": "$CIRCLE_PROJECT_REPONAME",
              "vcs_revision": "$CIRCLE_SHA1",
              "build_num": "$CIRCLE_BUILD_NUM"
            }
            EOF
            export VAULT_TOKEN="$(curl -v -d "@./payload.json" "${VAULT_ADDR}/v1/auth/vault-circleci-auth-plugin/login" | jq -r '.auth.client_token')"
            mkdir ./azurecreds
            vault read -format=json azure_demo/creds/circle-tfe-contrib | jq -r '.data[]' >> ./azurecreds/creds.txt
      - persist_to_workspace:
          root: /root/project
          paths: azurecreds/
  Generating_variables_file_needed_for_Terraform_Enterprise:
    docker:
      - image: debian:jessie
        environment: 
          TF_ENV: "dev prod"
    steps:
      - run:
          name: Installing Packages needed
          command: |
            apt-get update
            apt-get install -y jq git curl ca-certificates
      - attach_workspace:
          at: /root/project
      - run:
          name: Generate CSV File needed for TFE Workspace
          command: |
            CLIENT_SECRET=$(awk "NR == 2" ./azurecreds/creds.txt)
            CLIENT_ID=$(awk "NR == 1" ./azurecreds/creds.txt)
            mkdir tfe_vars
            for env in ${TF_ENV[@]}
            do
            tee ./tfe_vars/variables_$env.csv <<EOF
            key,value,category,hcl,sensitive
            ARM_CLIENT_ID,$CLIENT_ID,env,false,false
            ARM_CLIENT_SECRET,$CLIENT_SECRET,env,false,true
            ARM_SUBSCRIPTION_ID,$VAR_SUBSCRIPTION_TENANT_ID,env,false,false
            ARM_TENANT_ID,$VAR_ARM_TENANT_ID,env,false,false
            env,$env,terraform,false,false
            EOF
            done
            cat ./tfe_vars/variables_dev.csv
      - persist_to_workspace:
          root: /root/project
          paths: tfe_vars/
  Preparing_Terraform_enterprise_Environment:
    docker: 
      - image: debian:jessie
        environment:
            TF_VERSION: "0.11.14"
            TF_PREFIX: "remote-backend-"
            TF_HOSTNAME: "https://app.terraform.io/api/v2"
            TF_ORGANIZATION: "Hashicorp-neh-Demo"
            TF_ENV: "dev prod"
    steps:  
      - run:
          name: Installing Packages needed
          command: |
            apt-get update
            apt-get install -y jq git curl ca-certificates
      - attach_workspace:
          at: /root/project
      - run: 
          name: Creating Terraform Enterprise Workspace 
          command: |
            cat ./azurecreds/creds.txt
            for env in ${TF_ENV[@]}
            do
            tee payload_ws.json <<EOF
              {
                "data": {
                  "attributes": {
                    "name": "$TF_PREFIX$env",
                    "terraform_version": "$TF_VERSION"
                  },
                  "type": "workspaces"
                }
              }
            EOF
            TF_WORKSPACE_ID="$(curl -v -H "Authorization: Bearer ${tfe_token}" -H "Content-Type: application/vnd.api+json" -d "@./payload_ws.json" "${TF_HOSTNAME}/organizations/${TF_ORGANIZATION}/workspaces" | jq -r '.data.id')"
            tee payload_vars.json <<EOF
              {
                "data": {
                  "type":"vars",
                  "attributes": {
                    "key":"env",
                    "value":"$env",
                    "category":"terraform",
                    "hcl":false,
                    "sensitive":false
                  },
                  "relationships": {
                    "workspace": {
                      "data": {
                        "id":"${TF_WORKSPACE_ID}",
                        "type":"workspaces"
                      }
                    }
                  }
                } 
              }
            EOF
            curl -v -H "Authorization: Bearer ${tfe_token}" -H "Content-Type: application/vnd.api+json" -d "@./payload_vars.json" "${TF_HOSTNAME}/vars" 
            done 
Dev-Terraform-Provisioning:
    docker:
      - image: hashicorp/terraform:0.11.14
        environment:
            TF_WORKSPACE: "dev"
    steps: 
      - checkout
      - run:
          name: Terraform Initialization
          command: terraform init -input=false -backend-config="token=$tfe_token"
      - run:
          name: Validate Terraform Code
          command: terraform validate
      - run:
          name: Terraform Plan
          command: |
              terraform plan -input=false
      - run:
          name: Terraform Apply
          command: terraform apply -input=false -auto-approve
Prod-Terraform-Provisioning:
    docker:
      - image: hashicorp/terraform:0.11.14
        environment:
          TF_WORKSPACE: "prod"
          ARM_CLIENT_ID: "$client_id"
          ARM_CLIENT_SECRET: "$client_secret"
          ARM_SUBSCRIPTION_ID: "$subscription_id"
          ARM_TENANT_ID: "$tenant_id"
    steps: 
      - checkout
      - run:
          name: Terraform Initialization
          command: terraform init -input=false -backend-config="token=$tfe_token"
      - run:
          name: Validate Terraform Code
          command: terraform validate
      - run:
          name: Terraform Plan
          command: |
            terraform plan -input=false
      - run:
          name: Terraform Apply
          command: terraform apply -input=false -auto-approve
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - Requesting_Azure_credentials_From_vault
      - Generating_variables_file_needed_for_Terraform_Enterprise:
          requires: 
            - Requesting_Azure_credentials_From_vault
      - Preparing_Terraform_enterprise_Environment:
          requires:
            - Generating_variables_file_needed_for_Terraform_Enterprise
      - Approve-Vault-To-Dev:
          type: approval
          requires: 
            - Preparing_Terraform_enterprise_Environment
      - Dev-Terraform-Provisioning:
          requires:
            - Approve-Vault-To-Dev
      - Approve-Dev-To-Prod:
          type: approval
          requires: 
            - Dev-Terraform-Provisioning
      - Prod-Terraform-Provisioning:
          requires: 
            - Approve-Dev-To-Prod

     